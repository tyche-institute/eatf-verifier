# `mcp-tools-call-valid/`

**Expected:** `verify=true`, `overtReceipt.policy.decision == "allow"`.

A well-formed AEP for an MCP `tools/call` invocation that was
allowed by the deployment's policy. Exercises the
`agentic-extended:mcp-tools-call` OVERT scope.
